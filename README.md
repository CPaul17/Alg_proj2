In order to run this project you must first run the following commands:
pip install Pillow
pip install opencv-python
pip install numpy

or you can do pip install -r requirements

I highly recommend doing this in a virtual environment